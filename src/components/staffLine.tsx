import { Key } from "@/utils/types";
import NoteNode from "@/components/noteNode";

export default function StaffLine({key}: {key: Key}) {
  return (
    <div className="border-y border-secondary h-6">
    </div>
  )
}